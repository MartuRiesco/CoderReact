 const array1 = [5, 12, 8, 130, 44];

/* const found = array1.find(element => element > 10);

console.log(found);  */
for (let i = 0;i<array1.length; i++) {
  array1.forEach(element => {if(element>10){
    return array1
  }
    
  });
}